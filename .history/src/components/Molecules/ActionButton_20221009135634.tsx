import { Menu, MenuButton } from '@chakra-ui/react'
import React from 'react'

const ActionButton = () => {
  return (
    <MenuButton as={Butto} rightIcon={<ChevronDownIcon />}>
    Actions
  </MenuButton>
  )
}

export default ActionButton
